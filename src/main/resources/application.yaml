#DB Settings

server:
  error:
    include-stacktrace: always

spring:
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

datasource:
  url: jdbc:mysql://localhost:3306/hospital_appointment_management-db?createDatabaseIfNotExist=true
  username: root
  password: password
